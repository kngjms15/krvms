// schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("POSTGRES_PRISMA_URL") // uses connection pooling
  directUrl = env("POSTGRES_URL_NON_POOLING") // uses a direct connection
}

model Post {
  id        String  @id @default(cuid())
  title     String
  content   String?
  published Boolean @default(false)
  author    User?   @relation(fields: [authorId], references: [id])
  authorId  String?
}

model User {
  id        String   @id @default(cuid())
  name      String?
  email     String?  @unique
  createdAt DateTime @default(now()) @map(name: "created_at")
  updatedAt DateTime @updatedAt @map(name: "updated_at")
  posts     Post[]

  @@map(name: "users")
}

// model Roles {
//   id              Int        @id @default(autoincrement())
//   name            String     @db.VarChar(20)
//   description     String?    @db.VarChar(50)
//   permission      Int
//   active          Int?
//   created         DateTime?
//   lastModified    DateTime?
//   jobs            Job[]
//   specialities    Speciality[]
// }

// model Chapter {
//   id              Int             @id @default(autoincrement())
//   name            String          @db.VarChar(20)
//   address         String          @db.VarChar(50)
//   city            String          @db.VarChar(50)
//   province        String          @db.VarChar(2)
//   postalCode      String          @db.VarChar(6)
//   administrators  Administrator[]
// }

//do not modify! Under testing!
model VolunteerApplicant {
  applicantId                  Int      @id @default(autoincrement())
  createdAt                    DateTime @default(now())
  firstName                    String   @db.VarChar(50)
  lastName                     String   @db.VarChar(50)
  dob                          DateTime
  address                      String   @db.VarChar(50)
  cityInfo                     String   @db.VarChar(50)
  province                     String   @db.VarChar(2)
  postalCode                   String   @db.VarChar(6)
  chapter                      String   @db.VarChar(20)
  primaryPhone                 String   @db.VarChar(10)
  secondaryPhone               String?
  email                        String   @db.VarChar(50)
  employer                     String   @db.VarChar(50)
  conviction                   Boolean
  bondable                     Boolean
  medicalCondition             Boolean
  medicalConditionDetails      String?  @db.VarChar(500)
  emergencyContactName         String   @db.VarChar(50)
  emergencyContactRelationship String   @db.VarChar(50)
  emergencyContactPhone        String   @db.VarChar(10)
  volunteerExperienceDetails   String?  @db.VarChar(500)
  interviewStatus              String   @db.VarChar(20)
}

// model Event {
//   id             Int               @id @default(autoincrement())
//   name           String            @db.VarChar(50)
//   description    String?           @db.VarChar(100)
//   date           DateTime
//   location       String            @db.VarChar(50)
//   chapterId      Int
//   applications   Application[]
//   volunteerEvents VolunteerEvent[]
//   adminEvents    AdminEvent[]
// }

// model Volunteer {
//   id                     Int             @id @default(autoincrement())
//   firstName              String          @db.VarChar(50)
//   lastName               String          @db.VarChar(50)
//   DOB                    DateTime
//   address                String          @db.VarChar(50)
//   city                   String          @db.VarChar(50)
//   province               String          @db.VarChar(2)
//   postalCode             String          @db.VarChar(6)
//   chapter                String          @db.VarChar(20)
//   phoneNumber            String          @db.VarChar(10)
//   secondaryPhoneNumber   String?
//   email                  String          @db.VarChar(50)
//   employer               String          @db.VarChar(50)
//   conviction             Int
//   bondable               Int
//   medConditions          Int
//   medConditionsDetails   String         @db.VarChar(100)
//   emergName              String         @db.VarChar(50)
//   emergRelationship      String         @db.VarChar(50)
//   emergPhoneNumber       String         @db.VarChar(10)
//   chapterId              Int
//   specialities           Speciality[]
//   volunteerEvents        VolunteerEvent[]
// }

// model Administrator {
//   id              Int          @id @default(autoincrement())
//   firstName       String       @db.VarChar(50)
//   lastName        String       @db.VarChar(50)
//   email           String       @db.VarChar(50)
//   password        String       @db.VarChar(100)
//   permission      Int
//   chapterId       Int
//   chapter         Chapter      @relation(fields: [chapterId], references: [id])
//   jobs            Job[]
//   adminEvents     AdminEvent[]
// }

// model Job {
//   id            Int             @id @default(autoincrement())
//   adminId       Int
//   admin         Administrator   @relation(fields: [adminId], references: [id])
//   roleId        Int
//   role          Roles           @relation(fields: [roleId], references: [id])
// }

// model Speciality {
//   id            Int           @id @default(autoincrement())
//   volunteerId   Int
//   volunteer     Volunteer     @relation(fields: [volunteerId], references: [id])
//   roleId        Int
//   role          Roles         @relation(fields: [roleId], references: [id])
// }

// model Application {
//   applicantId   Int
//   applicant     Applicant     @relation(fields: [applicantId], references: [id])
//   eventId       Int
//   event         Event         @relation(fields: [eventId], references: [id])
//   @@id([applicantId, eventId])
// }

// model VolunteerEvent {
//   volunteerId   Int
//   volunteer     Volunteer     @relation(fields: [volunteerId], references: [id])
//   eventId       Int
//   event         Event         @relation(fields: [eventId], references: [id])
//   @@id([volunteerId, eventId])
// }

// model AdminEvent {
//   adminId       Int
//   admin         Administrator @relation(fields: [adminId], references: [id])
//   eventId       Int
//   event         Event         @relation(fields: [eventId], references: [id])
//   @@id([adminId, eventId])
// }
